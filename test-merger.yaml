name: test-merger
version: 1.0.0
description: Test minimal config to verify defaults merging

source:
  type: code
  adapter: typescript
  root: .
  include:
    - "src/**/*.ts"

neo4j:
  uri: ${NEO4J_URI}
  database: neo4j
  username: ${NEO4J_USERNAME}
  password: ${NEO4J_PASSWORD}
